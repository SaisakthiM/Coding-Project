[package]
name = "abstraction_bench"
version = "0.1.0"
edition = "2021"

[dependencies]
criterion = "0.5"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "iter_vs_manual"
harness = false

[profile.release]
lto = true
codegen-units = 1
