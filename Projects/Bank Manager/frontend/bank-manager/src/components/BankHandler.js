import axios from "axios";
export default function addMoney(amount) {
    try {
        API_KEY = none
        url = "http://localhost:8080/api/add/";
        params = {
            Authorization: `Bearer: ${API_KEY}`
        }
    }
    catch (e) {

    }
}   




































