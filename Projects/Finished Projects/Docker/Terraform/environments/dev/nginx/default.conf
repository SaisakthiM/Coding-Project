server {
    listen 80;
    client_max_body_size 1000M;
    server_name localhost;

    resolver 127.0.0.11 valid=30s ipv6=off;

    # ─── NOTES ───────────────────────────────────────────────
    # API calls: /notes/api/... → backend at /api/...
    location /notes/api/ {
        set $upstream http://notes-backend:8000;
        proxy_pass $upstream/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 2s;
        proxy_read_timeout 30s;
    }

    # Static frontend files served from /apps/notes/
    location /notes/ {
        root /apps;
        try_files $uri $uri/ /notes/index.html;
    }

    # ─── BANK ────────────────────────────────────────────────
    # API calls: /bank/api/... → backend at /api/...
    # Spring controller is at /api/auth and /api/accounts
    location /bank/api/ {
        set $upstream http://bank-backend:8080;
        proxy_pass $upstream/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 2s;
        proxy_read_timeout 30s;
    }

    # Static frontend files served from /apps/bank/
    location /bank/ {
        root /apps;
        try_files $uri $uri/ /bank/index.html;
    }

    # ─── QUIZ ────────────────────────────────────────────────
    location /quiz/ {
        root /apps;
        try_files $uri $uri/ /quiz/index.html;
    }

    # ─── VIDEO ───────────────────────────────────────────────
    location /video/api/ {
        set $upstream http://video-uploader-backend:8080;
        proxy_pass $upstream/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 2s;
        proxy_read_timeout 30s;
    }

    location /video/ {
        root /apps;
        try_files $uri $uri/ /video/index.html;
    }

    # ─── HOSPITAL ────────────────────────────────────────────
    # Django URLs are configured with hospital/ prefix already
    location /hospital/ {
        set $upstream http://hospital-management:8000;
        proxy_pass $upstream/hospital/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 2s;
        proxy_read_timeout 30s;
    }

    # ─── BLOG ────────────────────────────────────────────────
    # Django URLs start from / so pass full path including /blog/
    location /blog/ {
        set $upstream http://blog-website:8000;
        proxy_pass $upstream/blog/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 2s;
        proxy_read_timeout 30s;
    }

    # ─── SOCIAL MEDIA (kind cluster) ─────────────────────────
    location /social/ {
        set $upstream http://172.18.0.2:30080;
        proxy_pass $upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 2s;
        proxy_read_timeout 30s;
    }

    # ─── GATEWAY STATUS ──────────────────────────────────────
    location / {
        return 200 '{"status":"gateway running","apps":["/notes/","/bank/","/quiz/","/video/","/hospital/","/blog/","/social/"]}';
        add_header Content-Type application/json;
    }
}