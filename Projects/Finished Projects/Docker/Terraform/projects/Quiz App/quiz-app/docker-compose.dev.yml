services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: quiz-frontend
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "5173:5173"
    environment:
      - CHOKIDAR_USEPOLLING=true
