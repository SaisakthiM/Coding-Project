FROM node:20-bullseye
WORKDIR /app

# Copy package.json and package-lock.json first
COPY package*.json ./

# Clean install dependencies exactly as per lockfile
RUN npm ci

# Copy the rest of the app
COPY . .

EXPOSE 5173

CMD ["npm", "run", "dev"]
