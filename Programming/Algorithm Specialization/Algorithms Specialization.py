"""

This is MIT course on youtube

What is Algorithm : 
It is a method of sequence of continuous order to solve a particular problem or return the desired output
It can take arbitary sized input and should give a fixed output

To check if our Algorithm gave the correct outpot, we have to use 
(Let's take a algorithm or a function which inputs natural numbers and outputs even numbers)

1) Inductive hypothesis : We can also say or define the algorithm that if it can output the base case (we can see it shortly)
                          and doesn't have exceptions inside algorithm, this algorithm maps to correct output 100% of the time
                          if it does not return the base case, the algorithm has a error
2) Base Case : without the base case, we cannot determine what we cannot judge whether our output is right or not.
               for this algorithm, the base case is 1

Now, we have to check if the algorithm is efficient. 
there might be thousands of algorithm to solve a same problem but we have to use what is efficient
so let's take two worker who is a builder worker 1 and worker 2.
worker 1 is the output and worker 1 and 2 is input the algorithm is that pick who is the fastest.
how do you categorize who is faster you also given time they complete a work in min.
ofcourse you can take who has higher work per min.
but can you say the same for a body builder which calculates how much strong both are
no we calculate mou much highest weight lifted , not work per min

can you understand it, different algorithm has different sets of efficient calculation and differs from one another
in computer science algorithms, we calculate how much time it took to give that output and how much space it took to process that output
this is what we call Big(O) notation for time complexity and space complexity

Time complexity depends on CPU speed which varies for various devices
In PC's you heard of 32 bits 64 bits right 
like there will be a different version for 32 and 64 bits
do you know what is even 32 bit and 64 bit and why it is different
the answer is CPU operations. The program you install will need to compute different operations every second
they store it in RAM as bytes and send to computer each time because computer cannot process everything at the same time
the computer process it and sends to the application
if it is 32 bits it means 32 bit/s and it can cause crashing if program sends double the data

so what are the operations can we use. 
we can use integer operation, arithmetic, logical operations adn bit-wise operators

Space complexity depends on RAM storage which also varies for different devices

Big(O) is calculating worst-case scenario. this is what we call [Asymptotic Notations]

| Notation | Meaning   | Scenario                       |
| -------- | --------- | ------------------------------ |
| **O(n)** | Big O     | **Worst-case**                 |
| **Ω(n)** | Big Omega | **Best-case**                  |
| **Θ(n)** | Big Theta | **Average-case / Tight bound** |















































"""