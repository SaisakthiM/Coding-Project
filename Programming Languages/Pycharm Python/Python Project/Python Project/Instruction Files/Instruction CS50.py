# Terminal

# code ; it is used to read , write or create new file in terminal
# interpreter : it is translating device between computer and human which converts functions into binaries
# function ; it is some form of action which lets you to do something in program
#                or verb which are predetermined tasks or values which are useful to convey the message
#              between humans and computers

# arguments : it is an input to a fuction that somehow influence its behavior
# [eg : parathesis "" these are arguments helpful to use the function according to our wish]
#  [ "" takes as input some sort of string in english ultimately to print what you want in your screen]

# results such as printing is also known as side-effects , it can be visual or audio taht usually appear on screen due to our command
# in proramming mistakes in code are known as bug

# variable : which can store some value [string or number]

# return value [function] : lets assume return value as friend and
# we ask him to ask the name of his other friend (input)
# he tells that it is jack or any other name  (reply or)
# this is called return value

# these functions return user input so programmers can use it to do something

# assigment operator : they ar symbols like (=)
# which assigns a value or string to a variable
# it assigns from right to left whatever ther user input is
# = sign copies whatever left side had when right side is called

# comments : these are special symbol , if used it ignores the command whatever you type next
#   [ # and """ are used for commenting]
# pseudocode : these are just number of comments which are used for instructions
#            : these are written before code work so programmer can write by instruction

# eg ;
"""
this is a comment
"""

"""
strings : they are collections of letters [simply words] written under "" 
They are type of data in python

problem : to print user name
        : we could get input and store it and print outside the parathesis
        : but if we use [f] before parathesis and put the variable inside {} it actually prints the input rather than variable anme

+ : it is a symbol which are not only used for addition of 2 numbers 
But als for concantation of 2 strings

problem : to print your name after a line
        : there are 2 ways to solve
        : using print to print hello and next line your stored variable with your name
        
        : or we can use end='\n' which implies or instructs computer to point cursor in next line or print after that in next line
        : sep='' which leaves extra space or moves cursor a space after
        
there are some functions that can be used for strings

.strip : which actually removes extra space from string 
eg : name = name.strip()

conditonals : first we have to gige two object or anythin
then it checks whether the object fullfills the conditions are not [life if x<y]
if true it proceds 
if false then another command [else] 
"""