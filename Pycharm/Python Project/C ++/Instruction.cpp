/* This Is my First C++ Programming

Declaring Or Initialising a Variable :
For Initialising The Format is :
type filename = value;

For Printing :

There Are 2 Sub-Classes Inside printing In C++
int main(){
    a = 10;
    std:: cout

    this stands for standard output stream. Stream means sequence of character (They are Words in fancier Model for Computer). i this stream the sequence of character gets printed in standard output
}

/*#include <iostream>
using namespace std;

int main() {
    int file_size = 100;
    int counter = 1;
    double sales = 1020;
    cout << file_size << endl;
    cout << sales << endl;
    cout << counter << endl;
    return 0;
}

Program to Swap Integers
Note ; In C++ , The Variable can be Redeclare and Values Changes

#include <iostream>
using namespace std;

int main() {
    int a = 1;
    int b = 2;
    int c = a;
    a = b;
    b = c;
    cout << a << endl;
    cout << b << endl;
}

To Prevent a Variable Form change We Have To Convert it To Constant
It Is Done By The Function 'const'.
Eg ; #include <iostream>
using namespace std;

int main() {
    const int pi = 3.14;
    cout << pi;
}

Methods For Naming a Variable (Convention)

#include <iostream>
using namespace std;

int main() {
    const double pi = 3.14; // pi is Snake-Case
    const double Pi = 3.147; // Pi is Pascal-case
    const double pI = 3.1478; // pI is Camel-Case
    const double iPi = 3.14789; //The i Denotes Filetype and This Format is Known as Hungarian Notation
    cout << pi;
}

Mathematical Expression in C++

int main(){
    int a = 10
    int b = 20

    add = a+b
    sub = a-b
    mul = a*b
    div = a/b 
    / In division it becomes interesting because as we gave variable type int , it cannot fully divide and return teh value.
    So, we have to convert div variable to
    double div = a/b
    / and also any one of the variable
    mod = a%b / this returns the remainder of the division between variables


}

Increment and Decrement
It is nothing but adding values by 1 without using 1

#include <iostream>
using namespace std;

int main(){
    int a = 10;
    int b = a++; // here a become 11 and b becomes 10 because ++ (Increment) function is placed suffix. so the a will be incremented but b will not
    int c = ++a; // here c become 12 and a becomes 11 because ++ (Increment) function is placed prefix. so the c will be incremented but a will not
    std:: cout << c;
}

Order Of Operator , 
Like any other Programming Language , Ther is some instances like a = 1 + 7 * 4.
C++ Follows BODMAS Function to find values














































































































































































































































































































































































































*/
